# see https://github.com/QuentinTorg/esphome_custom_components for source of truth

packages:
  base_config: !include
    file: package.feit_dimmer.yaml
    vars:
      device_name: guest-bedroom-dimmer
      friendly_name: Guest Bedroom
      restore_default_brightness: 'true'
      linked_ha_outlet_entity_id: switch.guest_bedroom_light
